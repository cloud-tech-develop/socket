version: '3.4'

services:
  socket:
    image: socket
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - PORT=${PORT:-8889}
    ports:
      - "${PORT:-8889}:${PORT:-8889}"
